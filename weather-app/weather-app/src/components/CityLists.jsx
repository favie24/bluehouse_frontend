import { Clock, Star, X } from 'lucide-react'

function List({ title, icon: Icon, items, onSelect, onRemove }) { return <section className="glass rounded-3xl p-5"><div className="mb-4 flex items-center gap-2"><Icon size={17}/><h3 className="font-bold">{title}</h3></div>{items.length === 0 ? <p className="text-sm text-white/45">Nothing here yet.</p> : <div className="flex flex-wrap gap-2">{items.map(city => <div key={city} className="flex items-center rounded-full bg-black/10"><button onClick={() => onSelect(city)} className="px-4 py-2 text-sm hover:text-white">{city}</button>{onRemove && <button aria-label={`Remove ${city}`} onClick={() => onRemove(city)} className="pr-3 text-white/40 hover:text-white"><X size={14}/></button>}</div>)}</div>}</section> }
export default function CityLists({ favorites, recent, onSelect, removeFavorite }) { return <div className="grid gap-4 md:grid-cols-2"><List title="Favorite cities" icon={Star} items={favorites} onSelect={onSelect} onRemove={removeFavorite}/><List title="Recent searches" icon={Clock} items={recent} onSelect={onSelect}/></div> }
